How to use the website:

	User who is signued up (if not signed up thenm they need to) can log in and be sent to their personal web iteration.
	The them is set for the user according to the cookie and thus they can change it and it will remain in that mode untill changed. 
	The user can change between pages using the navbar. The user can also use the navbar to set their prefered genre/game style.
	The user will then next time they are loggied in will be shown their preference for that page ie, automatically applied filter.

Deafult Logins:

	username -> root@root.com
	password -> Root#123

Fucntionality not implemented:
	
	Task 4 -> rating the game using the slider, updating the top rated games using the rated table.
	Task 5 -> ran out of time.